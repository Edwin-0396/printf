#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{

  /*printf("Test: %d\n", _printf("%b", 98));*/
  _printf("%b + %b = %b\n", INT_MAX, INT_MAX);
	 
  return (0);
}
